# PROJECT INFO
project.name=zumo
project.title=Zumo
project.version=0.2
project.name.versioned=${project.name}-${project.version}

# LOCATIONS
src.dir=${basedir}/src
builder.dir=${basedir}/builder
yuicompressor.path=${builder.dir}/lib/yuicompressor-2.4.7.jar
output.dir=${basedir}/output
input.file=wrapper.js
output.file=${project.name}.js
release.file=${project.name.versioned}.js
release.file.min=${project.name.versioned}.min.js